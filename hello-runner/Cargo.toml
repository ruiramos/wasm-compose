[package]
name = "hello-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
cargo-component-bindings = "0.6.0"

[package.metadata.component]
target = { path = "../world.wit",  world = "runner" }

[package.metadata.component.dependencies]
